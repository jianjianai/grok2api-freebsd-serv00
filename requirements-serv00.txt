# Serv00 / Passenger / WSGI deployment dependencies.
# Install with the site interpreter, for example:
# /usr/local/bin/python3.12 -m pip install --user -r requirements-serv00.txt

a2wsgi>=1.10.10
aiofiles>=25.1.0
aiohttp>=3.13.3
aiohttp-socks>=0.11.0
certifi>=2026.1.4
cryptography>=46.0.5
fastapi>=0.119.0
loguru>=0.7.3
python-dotenv>=1.0.0
python-multipart>=0.0.21
websockets>=16.0

# curl-cffi does not support FreeBSD at install time.
# Serv00 deployments use the aiohttp-based compatibility fallback.
# orjson needs a newer Rust toolchain than Serv00 currently provides.
# FreeBSD deployments use the stdlib-backed compatibility shim in `orjson.py`.

# Optional storage backends:
# redis>=6.4.0
# sqlalchemy>=2.0.46
# greenlet>=3.3.1
# aiomysql>=0.2.0
# asyncpg>=0.31.0
